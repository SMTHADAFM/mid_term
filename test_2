//Schreibe ein kurzes Programm (statischer Modus), in dem eine int-Variable zahl gegeben ist. Das Programm soll überprüfen, ob

//zahl durch 5 teilbar ist (ohne Rest). Falls ja, soll das Programm "[zahl] ist durch 5 teilbar" in der Konsole ausgeben und weiter prüfen, ob ...
//zahl durch 10 teilbar ist (ohne Rest). Falls ja, soll das Programm "[zahl] ist durch 10 teilbar" in der Konsole ausgeben und weiter prüfen, ob ...
//zahl durch 15 teilbar ist (ohne Rest). Falls ja, soll das Programm "[zahl] ist durch 15 teilbar" in der Konsole ausgeben.
int zahl = 60;

if (zahl % 5 == 0) {
  println("zahl ist durch 5 teilbar");
}

if (zahl % 10 == 0) {
  println("zahl ist durch 10 teilbar");
}

if (zahl % 15 == 0) {
  println("zahl ist durch 15 teilbar");
}
