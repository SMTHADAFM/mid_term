//Gegeben sei Array foo. Drucke alle positiven Elemente untereinander in der Konsole aus. Der Code sollte ohne weitere Änderungen funktionieren, wenn du Zahlen zu foo hinzufügst oder entfernst. Verwende dafür eine Schleife.

int [] foo = {-2, 4, -3, 26, -122, 0, 10};

if (int i = foo; i >= 0;) {
  
  println(i);
}
